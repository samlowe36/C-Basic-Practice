#include <iostream>

int main() {
    const double pi = 3.14; //constants make it so variable value cant be changed. trying to give pi a new value while its a constant will give an error
    return 0;
}