#include <iostream>

int main() {
    int file_size = 100; //variable practice. this is an int. it is initialized with a value of 100
    double sales = 9.99; //a double is like a float but with 8 decimals instead of 4
    //std::cout << "Hello World!"; // prints hello world
    std::cout << file_size; //prints file_size value
    return 0;
}